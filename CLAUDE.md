# Mandatory Read Before Changes

Claude Code must always read `common.md` before modifying any files in this repository. This is a strict requirement. The `common.md` document defines the system architecture, the core agents, simulated data tools, and UI surface area. If `common.md` is not read first, you must stop and read it before proceeding with any edits, refactors, or file creation.
